<%- include("../partials/header") %>
<link rel="stylesheet" href="/css/achievement.css">
<div class="achievement-container">

  <!-- 🟦 LEFT SIDE -->
  <div class="overview-panel">

    <h3>Overview</h3>

    <div><strong>Total Students:</strong> <%= totalStudents || 0 %></div>
    <div><strong>Boys:</strong> <%= boys || 0 %></div>
    <div><strong>Girls:</strong> <%= girls || 0 %></div>

    <hr>

    <h4>Selections</h4>
    <div>SSC GD: <%= sscCount || 0 %></div>
    <div>CG Police: <%= cgPoliceCount || 0 %></div>
    <div>CTET: <%= ctetCount || 0 %></div>

  </div>

  <!-- 🟩 RIGHT SIDE -->
  <div class="achievement-content">

    <div class="achievement-grid">

      <% if (achievements && achievements.length > 0) { %>

        <% achievements.forEach(item => { %>

          <!-- ✅ WRAPPER (VERY IMPORTANT) -->
 <div class="achievement-card-wrapper">

  <div class="certificate" id="certificate-<%= item._id %>">

    <h3 class="title">Congratulations</h3>

    <div class="photo-wrapper">
      <img src="/api/upload/<%= item.studentId?.photoId %>" class="profile" />
    </div>

    <p class="quote">
      From Classroom Walls <br> to Success Halls
    </p>

    <h2 class="name"><%= item.studentId?.name %></h2>

    <div class="exam">
      <%= item.examQualified %>
    </div>

    <p class="batch">CG VYAPAM BATCH</p>

  </div>  <!-- ✅ certificate close -->

</div>    <!-- ✅ wrapper close -->

        <% }) %>

      <% } else { %>

        <p>No achievements found</p>

      <% } %>

    </div>

  </div>

</div>

<%- include("../partials/footer") %>